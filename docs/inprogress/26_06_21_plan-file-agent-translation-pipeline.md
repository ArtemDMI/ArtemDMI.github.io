# ВНИМАНИЕ! ГЛАВНАЯ ИНСТРУКЦИЯ ПО ВЫПОЛНЕНИЮ ПЛАНА. ВЫПОЛНЯЙ:
- Реализуй все незавершенные пункты [ ] сверху вниз.
- Если тебе назначили диапазон пунктов, выполняй только этот диапазон, но сохраняй общий порядок плана.
- После успешного выполнения пункта отметь его как [x].
- Если пункт содержит вложенные подпункты, заверши вложенные подпункты до отметки родительского пункта.
- Если встретил {{{USER}}}, задай пользователю указанный вопрос или попроси указанное действие, дождись ответа и только потом продолжай.
- Не меняй архитектурные решения без нового блокера и явного согласования.
- Не подменяй результат подготовкой, исследованием, болванками, частичной интеграцией или отчётом о намерениях. Пункт закрыт только когда выполнен его реальный эффект и есть проверка этого эффекта.
- Прочти весь этот план для понимания контекста.
---
# Контекст
- Цель: перестроить `cursor_api/file_agent` в личный CLI-пайплайн для перевода больших текстовых файлов через параллельные локальные Cursor Agent запуски модели `composer-2.5`.
- Основной рабочий сценарий:
  - пользователь просит модель обработать файл, например `sources/15/test.txt`;
  - модель запускает из `cursor_api`: `python -m file_agent run sources/15/test.txt -t`;
  - CLI нормализует исходный файл, делит его на части по границам предложений и сохраняет части в ту же папку;
  - первая часть остается в исходном файле `test.txt`;
  - остальные части создаются рядом с суффиксами текущего формата `testS001.txt`, `testS002.txt`, ...;
  - каждая часть параллельно отправляется отдельному Cursor Agent;
  - агент переводит часть на русский и упрощает текст по промпту из `cursor_api/file_agent/prompt.md`;
  - ответ агента заменяет содержимое того же файла части;
  - CLI ждет все параллельные части, сохраняет все успешные результаты и только потом возвращает общий статус.
- Второй рабочий сценарий:
  - после ручной проверки частей пользователь просит собрать файл;
  - модель запускает из `cursor_api`: `python -m file_agent run sources/15/test.txt -merge`;
  - CLI находит `test.txt` + `testS001.txt`, `testS002.txt`, ... в порядке номеров и перезаписывает исходный `sources/15/test.txt` объединенным текстом.
- Текущий код:
  - `cursor_api/file_agent/cli.py` сейчас требует `-t/--task` с текстом задачи, это нужно заменить на флаг режима перевода.
  - `cursor_api/file_agent/runner.py` уже запускает локального агента через `cursor-sdk-bridge` и модель `composer-2.5`, но обрабатывает файлы как обычную задачу.
  - `cursor_api/file_agent/normalization.py` умеет нормализовать текст в предложения и мерджить список файлов, но его контракт не совпадает с новым CLI.
  - `cursor_api/file_agent/split.py` содержит старый самостоятельный скрипт разбиения, его нужно адаптировать в модуль пайплайна.
  - `cursor_api/file_agent/prompt.md` содержит единственный редактируемый пользователем промпт перевода.
  - `cursor_api/file_agent/keys.py` читает API-ключ из внешнего текстового файла. Этот файл и путь не менять.
  - `cursor_api/README.md` сейчас короткая шпаргалка, ее нужно заменить подробной инструкцией пользователя по новому процессу.
- Архитектурные решения пользователя:
  - Не использовать бэкапы.
  - Не использовать фоллбеки.
  - Не переключаться на другие модели.
  - Не добавлять скрытые запасные режимы.
  - Ошибка должна быть ошибкой, а не автоматической попыткой угадать другой путь.
  - Модель для работы только `composer-2.5`.
  - Ключ хранится только во внешнем текстовом файле, `keys.py` не трогать без явного запроса.
  - На каждую попытку агента отводится 60 секунд.
  - На каждую часть максимум 3 попытки.
  - Если часть упала, остальные части все равно должны продолжать работу.
  - Успешные части должны быть сохранены даже при общем ошибочном завершении.
  - После всех попыток CLI должен кратко вывести, какие файлы успешны и какие упали.
  - Проверка размера: считать символы без пробелов по каждой исходной части до перевода и после перевода; допустимый диапазон `60%..140%`.
  - Деление: ориентир `150` строк на часть после нормализации и примерно `5000` символов на часть; границы частей должны идти по предложениям.
  - `prompt.md` нужно использовать как system prompt. Если при тестировании SDK явно не поддержит такой формат, не делать скрытый фоллбек, а остановиться с ошибкой и согласовать переход на обычный user prompt отдельно.
- Решение по служебным файлам:
  - Не добавлять manifest, sidecar, backup или скрытые временные копии.
  - Порядок merge восстанавливать по имени исходного файла и суффиксам `S001`, `S002`, ...
  - Исходные размеры частей для проверки держать в памяти в рамках текущего запуска `-t`.
  - Консольный вывод использовать как главный отчет для пользователя.
- Работать только внутри:
  - `cursor_api/README.md`
  - `cursor_api/file_agent/cli.py`
  - `cursor_api/file_agent/runner.py`
  - `cursor_api/file_agent/normalization.py`
  - `cursor_api/file_agent/split.py`
  - новые модули внутри `cursor_api/file_agent/`, если они реально подключены к CLI
  - тесты или локальные проверочные скрипты внутри `cursor_api/`, если нужны для доказательства результата
- Не трогать:
  - `cursor_api/file_agent/keys.py`
  - внешний файл ключа
  - пользовательские файлы `sources/*` во время реализации, кроме осознанной ручной проверки на копии или специально созданном тестовом файле,



  @cursor_api/README.md твой задача сделать подробный план 
.agents/skills/plan/plan-GOOD.md - (читай и следуй инструкции)

CLI должен работать теперь вот так:

1. просит нейросеть "обработай вот этот файл sources\15\test.txt"
2. модель делет команду "python -m file_agent run sources/15/test.txt -t
3. включается cursor_api\file_agent\normalization.py (адаптируй) и проводится нормализация текста test.txt
4. файл рабивается на части по границам предложений cursor_api\file_agent\split.py 150 строк на файл с суфиксами и все файлы сохраняются в туже папку 
5. каждый из файлов паралельно каждым агентом переводдится на русский и упрощается по промпту "cursor_api\file_agent\prompt.md" (тест из этого файла вставляем в "system prompt":"plasholder" хранить промт только в этом файле)
6. скрипт ждет 60 секунд каждого анета и если пришел ответ то возращает и заменяет файл части откуда был взять часть текста. если не пришел делает 3 попытки и если не пришел, ждем сохраняем остальных и останавливаем скрипт с ошибкой
7. если все файлы переведены то отдельным скриптом проверяем количество символов в каждом файле с исходным если оно не отличается на 40% то скрипт завершается успешно иначе с ошибкой. Главный принцип скрипта - сохранить все что нужно. Если у нас какой-то файл или какая-то система сопротивляется остальные параллельные части мы не удаляем ждём и бережно сохраняем все что можем сохранить даже если об То есть из 40 параллельных агентов несколько завершились с ошибкой все остальные должны быть сохранены в файле. Пользователь сам вручную решит эту проблему
8. Пользователь удостоверяется что все работает , все хорошо перевелось и проверяет каждый файл. Смотрит ошибки консоли и где какие части поломались и нуждаются в ручной доработке 
9. Пользователь редактирует файлы вручную и сохраняет изменения в файлы.
10. Пользователь просит нейросеть "Собери в единый файл test.txt"
11. модель делет команду "python -m file_agent run sources/15/test.txt -merge"
12. скрипт собирает все файлы в единый файл test.txt и сохраняет В изначальный файл  sources/15/test.txt


Итог. Краткий вывод. Пользователь на первом этапе позволяет модели разломать части на куски и запустить в работу каждый из нормализированных пакетов со специальным промптом который пользователь в будущем будет менять и оно отсылается на сервер модель называется Composer 2.5 при помощи Cursor AI API Затем скрипт параллельно ожидает пока ответят все агенты то есть все запросы. И постепенно приходящие части заменяет в текст тех частей из тех файлов текст который мы получаем В те же файлы откуда текст был взят После того как все части успешно сохранились пользователь их осмотрит проверить почитает логи консолей если были где-то ошибки и повторы или количество текста или строк не соответствует Понятно что английский текст и русский текста будут отличаться по количеству символов, но без пробелов мы должны посчитать и примерно 40% мы пропускаем. Если меньше то есть отстает разница больше чем на 40% это означает что мы даем сигнал. пользователю в консоль То есть в конце очень кратко мы даем сводку пользователю. В каких файлах были ошибки он сам посмотрит и сделает это После этого пользователь опять попросит собрать в единый файл готовый структуру и скрипт при помощи команды cli Должен собрать это в единый изначальный файл. В итоге пользователь будет получать русский перевод по своему промпту. И сворачивать их обратно. Сворачивать и разворачивая нужно для того чтобы можно было параллелить объект. Каждый из агентов не получал большое количество техники так как он тупеет из-за этого и не отдает нужное количество текста. поэтому 150 строк либо какое то определенное количество символов нам подойдёт пока что 

Также твоя задача подготавливать любые другие системы, которые будут сопутствовать с этим. Писать новые скрипты, адаптировать старые и так далее. Так как сейчас курсор API C:\PROJECTS\ArtemDMI.github.io\cursor_api И его скрипты, которые я указал состоят из мусора. Это другие разные системы, которые... Я проколлекционировал , собрал из других частей они не настроены вместе хоть и лежат в одной папке поэтому твоя задача - настроить весь процесс чтобы все шло гладко и как и задумано в этой инструкции Обязательно выполняя всю последовательность задач. Хоть в скриптах может написано как-то по другому. Используя если что документацию в этой же папке чтобы узнать как работает API Сейчас твоя задача проанализировать это и задать вопросы если что-то непонятно Перед тем как создавать какой-либо план 


---
## Критерий завершения

Работу можно считать законченной, когда одновременно верны все пункты:

1. `python -m file_agent run <file> -t` запускает полный режим: нормализация → разбиение → параллельный перевод частей → сохранение успешных частей → проверка размера → краткая сводка.
2. `python -m file_agent run <file> -merge` собирает `<file>` + `<name>S001<ext>` + `<name>S002<ext>` в исходный `<file>` в правильном порядке.
3. `-t` больше не требует текст задачи и всегда читает инструкцию только из `cursor_api/file_agent/prompt.md`.
4. Каждая часть обрабатывается отдельным агентом `composer-2.5`, с лимитом `60` секунд на попытку и максимум `3` попытками на часть.
5. Ошибка одной или нескольких частей не отменяет ожидание и сохранение остальных успешных частей.
6. После обработки CLI завершает процесс с кодом `0`, только если все части успешно обработаны и прошли проверку `60%..140%` по символам без пробелов.
7. `cursor_api/README.md` содержит точную пользовательскую инструкцию: как запускать перевод, как читать ошибки, как вручную править части и как запускать merge.
8. Минимальная локальная проверка подтверждает разбиение, merge, проверку размеров и поведение при частичной ошибке без реального расхода API, а реальный агентский запуск оставлен отдельной ручной проверкой пользователя.

---
# ПЛАН:
Сначала зафиксируй текущий интерфейс и преврати старые разрозненные скрипты в один подключенный CLI-пайплайн. Не создавай неподключенные заготовки: каждый новый модуль должен быть вызван из `cli.py` или `runner.py`.

[x] Прочитай текущие файлы `cursor_api/file_agent/cli.py`, `runner.py`, `normalization.py`, `split.py`, `prompt.md`, `bridge_launch.py`, `README.md` и зафиксируй в рабочей заметке для себя, какие функции можно переиспользовать без изменения поведения.

[x] Зафиксируй целевую структуру кода и соблюдай ее при реализации:

    ├─ cursor_api/README.md — подробная инструкция пользователя по новому CLI
    ├─ cursor_api/file_agent/cli.py — разбор команд `run <file> -t` и `run <file> -merge`
    ├─ cursor_api/file_agent/runner.py — запуск одного Cursor Agent для одной части
    ├─ cursor_api/file_agent/normalization.py — нормализация текста перед разбиением
    ├─ cursor_api/file_agent/split.py — разбиение и поиск частей по суффиксам
    ├─ cursor_api/file_agent/pipeline.py — основной поток translate/merge, параллельность, сводка
    ├─ cursor_api/file_agent/validation.py — подсчет символов без пробелов и проверка `60%..140%`
    └─ cursor_api/file_agent/prompt.md — единственный пользовательский промпт перевода

    **Граф вызовов (единственный вход — `python -m file_agent`):**

    ```
    cli.main
      └─ run <file> -t  → pipeline.run_translate(Path)
      └─ run <file> -merge → pipeline.run_merge(Path)

    pipeline.run_translate
      ├─ read UTF-8
      ├─ normalization.normalize_text
      ├─ split.split_normalized → split.write_parts (или эквивалент: первая часть в source, остальные S00N)
      ├─ validation.count_non_whitespace (на исходных частях, в памяти процесса)
      ├─ runner.run_part (параллельно, до 3 попыток × 60 с на часть)
      ├─ validation.validate_translation (после успеха агента)
      └─ сводка в stdout, exit 0 только если все части OK

    pipeline.run_merge
      ├─ split.find_existing_parts (source + S001, S002, … без разрывов)
      ├─ read UTF-8 всех частей
      ├─ join через "\n\n" (один зафиксированный формат)
      └─ перезапись source, суффиксные файлы не удалять
    ```

    **Публичный контракт по модулям (реализовать в следующих пунктах, не менять роли):**

    | Модуль | Ответственность | Публичный API |
    |--------|-----------------|---------------|
    | `cli.py` | argparse, UTF-8 stdout/stderr, маршрутизация режимов | `build_parser()`, `main(argv) -> int` |
    | `pipeline.py` | оркестрация `-t` / `-merge`, параллельность, сводка, exit code | `run_translate(source: Path) -> int`, `run_merge(source: Path) -> int` |
    | `runner.py` | один агент на один файл-часть | `run_part(part_path: Path, *, system_prompt: str, timeout: float = 60) -> None` (ошибка — исключение, без fallback) |
    | `normalization.py` | текст → строки-предложения, без записи на диск | `normalize_text(text: str) -> str` (пустой/без предложений → явная ошибка) |
    | `split.py` | деление, пути S00N, поиск частей для merge, конфликты | `split_normalized(text) -> list[str]`, `part_paths(source, extra_count) -> list[Path]`, `write_parts(source, parts) -> list[Path]`, `find_existing_parts(source) -> list[Path]`, `check_split_conflicts(source, paths) -> None` |
    | `validation.py` | проверка длины перевода | `count_non_whitespace(text) -> int`, `validate_translation(source, translated) -> ValidationResult` |
    | `prompt.md` | единственный редактируемый промпт | читается `runner`/`pipeline`, передаётся как system prompt |
    | `README.md` | пользовательская инструкция | описывает ровно подключённые команды и лимиты |

    **Вспомогательные модули (не в дереве, не менять без явного запроса):**

    - `bridge_launch.launch_bridge_client` — bridge + `CursorClient`, timeout 60 с.
    - `keys.load_api_key` — внешний файл ключа.

    **Что убрать / не тащить в новый контракт:**

    - `cli`: `-t "текст"`, несколько файлов, `--no-stream`.
    - `runner`: `process_files`, мультифайловый `_build_prompt`.
    - `normalization`: CLI `main` / `run_merge` / `run_sentences` как отдельные entrypoint (логику merge/sentences — в библиотечные функции).
    - `split`: standalone `main`, `is_already_processed`, массовый обход `sources/`, лимит 7000, `utf-8-sig`.

    **Новые файлы `pipeline.py` и `validation.py`:** создаются и подключаются из `cli.py` в своих пунктах плана; на этом шаге только контракт выше, без неподключённых заготовок.

[x] Обнови `cursor_api/file_agent/cli.py` так, чтобы команда `run` принимала ровно один исходный файл и два взаимоисключающих режима:
    [x] `python -m file_agent run sources/15/test.txt -t` запускает режим перевода.
    [x] `python -m file_agent run sources/15/test.txt -merge` запускает режим сборки.
    [x] Старый режим `-t "текст задачи"` убери, потому что текст задачи теперь всегда лежит в `prompt.md`.
    [x] Если пользователь передал оба режима, ни одного режима или больше одного файла, CLI должен завершиться ошибкой argparse.

[x] Подготовь нормализацию в `cursor_api/file_agent/normalization.py` как библиотечную функцию для пайплайна:
    [x] Сохрани смысл текущей нормализации: убрать пустые строки, схлопнуть переносы, привести текст к строкам-предложениям.
    [x] Функция должна принимать текст и возвращать нормализованный текст без записи на диск.
    [x] Если текст пустой или предложения не найдены, функция должна бросать явную ошибку.
    [x] Не добавляй автоматическую коррекцию кодировок, backup-файлы или скрытые режимы.

[x] Адаптируй `cursor_api/file_agent/split.py` под новый контракт:
    [x] Реализуй функцию разбиения нормализованного текста на части по границам предложений.
    [x] Ограничь часть одновременно двумя ориентирами: не больше `150` строк и примерно не больше `5000` символов.
    [x] Если одно предложение само длиннее лимита, не режь его посередине; положи его в отдельную часть и дай предупреждение в консоль.
    [x] Реализуй генерацию путей `test.txt`, `testS001.txt`, `testS002.txt`, ...
    [x] Реализуй поиск существующих частей для merge: исходный файл + подряд идущие суффиксы по возрастанию номера.
    [x] Не удаляй старые части автоматически. Если старые суффиксные файлы конфликтуют с новым разбиением, заверши процесс ошибкой и сообщи пользователю список конфликтов.

[x] Создай `cursor_api/file_agent/validation.py`:
    [x] Реализуй подсчет символов без пробелов для строки.
    [x] Реализуй проверку пары `source_text`/`translated_text`: перевод проходит, если отношение находится в диапазоне `0.60..1.40`.
    [x] Если исходная часть имеет `0` символов без пробелов, это ошибка входных данных.
    [x] Верни структурированный результат для сводки: файл, исходный размер, итоговый размер, отношение, статус.

[x] Перестрой `cursor_api/file_agent/runner.py` под обработку одной части:
    [x] Оставь модель строго `composer-2.5`.
    [x] Оставь чтение API-ключа через `load_api_key()` из `keys.py`; сам `keys.py` не изменяй.
    [x] Реализуй запуск агента для одного файла части.
    [x] Передай текст `prompt.md` как system prompt через поддерживаемый SDK message/API формат.
    [x] User-сообщение агента должно быть коротким: обработай конкретный файл на диске, переведи на русский и упрости согласно system prompt, затем перезапиши этот же файл.
    [x] Если SDK на тесте не принимает system prompt, не делай автоматический user-prompt fallback. Заверши с явной ошибкой, где написано, что нужно согласовать изменение контракта.
    [x] Добавь таймаут `60` секунд на одну попытку.
    [x] Не добавляй фоллбек на другую модель, другой ключ, другой workspace или другой prompt-файл.

[x] Создай `cursor_api/file_agent/pipeline.py` и подключи его из `cli.py`:
    [x] В режиме `-t` прочитай исходный файл в UTF-8.
    [x] Нормализуй текст через `normalization.py`.
    [x] Разбей текст через `split.py`.
    [x] Перед записью частей посчитай размер каждой исходной части без пробелов и сохрани эти значения в памяти текущего процесса.
    [x] Перезапиши исходный файл первой частью.
    [x] Создай остальные части рядом с исходным файлом с суффиксами `S001`, `S002`, ...
    [x] Запусти обработку всех частей параллельно.
    [x] Для каждой части сделай максимум `3` попытки по `60` секунд.
    [x] Как только часть успешно обработана агентом, прочитай ее с диска и проверь размер через `validation.py`.
    [x] Если часть успешно обработана и прошла проверку, отметь ее успешной в памяти и больше не трогай.
    [x] Если часть упала по агенту, таймауту или проверке размера, сохрани файл в текущем состоянии и зафиксируй ошибку в сводке.
    [x] Дождись завершения всех частей даже после первых ошибок.
    [x] В конце напечатай краткую сводку: всего частей, успешно, ошибки, список проблемных файлов с причиной.
    [x] Верни код `0` только когда все части успешны; иначе верни ненулевой код.

[x] Реализуй режим `-merge` в `pipeline.py`:
    [x] Найди исходный файл и суффиксные части рядом с ним.
    [x] Отсортируй части строго по номеру суффикса.
    [x] Прочитай все найденные части в UTF-8.
    [x] Объедини их в один текст через пустую строку или один перенос строк; выбери один формат и зафиксируй его в README.
    [x] Перезапиши исходный файл объединенным текстом.
    [x] Не удаляй суффиксные файлы после merge.
    [x] Если есть разрыв в нумерации, например есть `S001` и `S003`, но нет `S002`, заверши merge ошибкой.
    [x] Напечатай краткую сводку: сколько файлов собрано и куда записан результат.

[x] Обнови `cursor_api/README.md` как рабочую инструкцию для пользователя:
    [x] Объясни, что команды запускать из каталога `cursor_api`.
    [x] Покажи команду перевода: `python -m file_agent run sources/15/test.txt -t`.
    [x] Покажи команду сборки: `python -m file_agent run sources/15/test.txt -merge`.
    [x] Опиши, что первая часть остается в исходном файле, остальные появляются рядом с суффиксами.
    [x] Опиши, что `prompt.md` является единственным местом для редактирования промпта.
    [x] Опиши лимиты: `150` строк, примерно `5000` символов, `3` попытки, `60` секунд.
    [x] Опиши проверку `60%..140%` по символам без пробелов.
    [x] Опиши поведение при частичных ошибках: успешные части сохраняются, общий процесс завершается ошибкой, пользователь вручную смотрит проблемные файлы.
    [x] Прямо напиши, что backup, fallback и смена модели не используются.
    [x] Прямо напиши, что ключ находится во внешнем текстовом файле и `keys.py` не нужно менять.

[x] Добавь минимальные проверки без реального API:
    [x] Проверь нормализацию на коротком английском тексте с переносами и пустыми строками.
    [x] Проверь разбиение: часть не превышает `150` строк, а обычные части держатся около `5000` символов.
    [x] Проверь генерацию путей `test.txt`, `testS001.txt`, `testS002.txt`.
    [x] Проверь merge-порядок и ошибку при разрыве суффиксов.
    [x] Проверь validation: успешный диапазон `0.60..1.40`, ошибка ниже `0.60`, ошибка выше `1.40`.
    [x] Проверь частичную ошибку через fake runner: одна часть падает, остальные сохраняются, итоговый код ненулевой.

[x] Проведи ручную smoke-проверку CLI без реального API там, где возможно:
    [x] Запусти help: `python -m file_agent --help`.
    [x] Запусти help run: `python -m file_agent run --help`.
    [x] На временном тестовом файле проверь `-merge` без обращения к API.
    [x] Не запускай реальный перевод на пользовательских `sources/*` без явного согласия пользователя.

[x] Подготовь финальную проверку реального сценария для пользователя:
    [x] В README укажи, что пользователь сам запускает первый реальный перевод на выбранном файле.
    [x] Объясни, что если system prompt не поддержится SDK, CLI должен показать явную ошибку и работу нужно остановить.
    [x] Объясни, что после успешного перевода пользователь вручную проверяет части и затем запускает `-merge`.

[x] Перед завершением сверить ограничения:
    [x] `keys.py` не изменен.
    [x] Нет backup-файлов.
    [x] Нет manifest/sidecar-файлов.
    [x] Нет fallback на другую модель.
    [x] Нет fallback на user prompt без отдельного решения пользователя.
    [x] `README.md` описывает ровно тот процесс, который реально подключен в коде.
    [x] Все новые функции вызываются из CLI-потока, а не лежат неподключенными заготовками.
